{
  "name": "@cognotate/cli",
  "version": "0.1.0",
  "description": "CLI application for Cognotate",
  "type": "module",
  "scripts": {
    "dev": "bun index.ts",
    "build": "bun build index.ts --target=bun --outdir ./dist --metafile=./dist/meta.json --metafile-md=./dist/meta.md",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "catalog:types"
  },
  "peerDependencies": {
    "typescript": "catalog:types"
  },
  "dependencies": {
    "@cognotate/core": "workspace:*",
    "@inquirer/prompts": "^8.2.0",
    "commander": "^14.0.3",
    "elysia": "^1.4.25",
    "zod": "catalog:"
  }
}
